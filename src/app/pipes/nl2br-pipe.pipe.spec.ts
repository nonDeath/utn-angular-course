import { Nl2brPipePipe } from './nl2br-pipe.pipe';

describe('Nl2brPipePipe', () => {
  it('create an instance', () => {
    const pipe = new Nl2brPipePipe();
    expect(pipe).toBeTruthy();
  });
});
